/**
 * Monoid: Algebraic Structure
 * - Closure: a, b from A. then a * b is in A too.
 * - Identity: for every element in A, they have identity element in A (name "i") that make a * i = i * a = a
 * - Associativity: a, b, c from A, then (a * b) * c = a * (b * c)
 */


